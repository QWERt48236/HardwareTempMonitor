<Window x:Class="HardwareTempMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:HardwareTempMonitor.ViewModels" 
        xmlns:local="clr-namespace:HardwareTempMonitor.Views" 
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:CPUViewModel/>
    </Window.DataContext>
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel>
            <Expander Header="CPU" Margin="10, 15">
                <StackPanel>
                    <Button Margin="10" Command="{Binding CPUTemperatureCommand}">Temperature</Button>
                    <Button Margin="10" Command="{Binding CPULoadCommand}">Load</Button>
                </StackPanel>
            </Expander>

            <Expander Header="MotherBoard" Margin="10">
                <StackPanel>
                    <Button Margin="10">Temperature</Button>
                    <Button Margin="10">Load</Button>
                </StackPanel>
            </Expander>

            <Expander Header="RAM" Margin="10">
                <StackPanel>
                    <Button Margin="10">Temperature</Button>
                    <Button Margin="10">Load</Button>
                </StackPanel>
            </Expander>
            
        </StackPanel>

        <local:CPUControl Grid.Column="1"/>
    </Grid>
</Window>
